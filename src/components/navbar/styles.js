import { makeStyles } from "@material-ui/core/styles";

const useStyles = makeStyles((theme) => ({
  appBar: {
    backgroundColor: "#FFFFFF",
    color: "#3466AF",
    borderBottom: "3px solid #3466AF",
  },
  title: {
    flexGrow: 1,
    textAlign: "center",
  },
}));

export default useStyles;
